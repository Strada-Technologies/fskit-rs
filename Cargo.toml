[package]
name = "fskit-rs"
version = "0.1.0"
edition = "2024"

[dependencies]
thiserror = "2.0"
tempfile = "3.20"
tokio = { version = "1.47", features = ["net", "rt", "rt-multi-thread", "macros", "process", "sync"] }

[dev-dependencies]
futures = "0.3"
